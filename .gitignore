# Rust
/target/
# Keep Cargo.lock for reproducible builds (this is an application, not a library)

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Environment
.env
.env.local

# Claude Code context (local only)
CLAUDE.md

# Internal feature specs (not public)
docs/features/

# Local sibling repos (not part of this project)
visigrid-site/
visihub-api/
visihub-app/
visihub-site/

# Distribution repos (managed separately)
homebrew-tap/
winget-manifests/
